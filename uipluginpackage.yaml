# Foundation — OpenSphere subShell (SDK 표준 빈 골격). _skeleton에서 인스턴스화.
# manifest.sha256는 서명 핀 — 서명 시 자동 갱신.
# 감사 시정 S1(2026-07-06): image.digest는 sha256 필수(태그 금지, CRD pattern + controller InvalidDigest 이중 강제).
apiVersion: plugins.opensphere.io/v1alpha1
kind: UIPluginPackage
metadata:
  name: foundation
  namespace: opensphere-system
spec:
  displayName: "Foundation"
  owner: platform-experience
  version: 0.1.0
  description: "Foundation — OpenSphere subShell (SDK 표준 골격)"
  image:
    repository: ghcr.io/opensphere-platform/foundation
    digest: sha256:c6933b413ffafb232f8166ed1f85a33f0bf376350f078c1dfaf67038e742fe96  # fss-engine-catalog-syncope-scim-v2-20260706-2325
  nav:
    band: "운영 Operate"
    label: "Foundation"
  manifest:
    path: /plugins/ui-shell.manifest.json
    sha256: "cf17a2b66ea6f02e9551373a7311788006b69d09ffc5f398d4c8781f1266bf08"
    signaturePath: /plugins/ui-shell.manifest.json.sig
  trust:
    keyId: opensphere-plugins-v2
  shellCompat: ">=0.2.0 <0.9.0"
  permissions:
    - page:register
    - api:proxy
  api:
    basePath: /api/plugins/foundation
---
apiVersion: plugins.opensphere.io/v1alpha1
kind: UIPluginRegistration
metadata:
  name: foundation
  namespace: opensphere-system
spec:
  packageRef:
    name: foundation
  desiredState: Enabled
  installPolicy:
    createWorkload: true
    createProxyRoute: false
    exposeInNavigation: true
  approval:
    requestedBy: cmars
    reason: "Foundation subShell (SDK 표준 골격)"
